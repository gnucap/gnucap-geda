For lang_geda (gEDA plugin)

**TODO (In the order of preference)
-(BUG) precalc_first is 'printing parameter <basename> has no value' : To Fix!
-Parse and print heirarchical schematic (using parse_module and print_module)
-Create augmented verilog netlist from a verilog netlist
-cleanup gschem command arguments

**optional/later
-Hack a button/option into gschem to get a netlist in verilog from gnucap
-support more gEDA symbols
-global nodes?
-gEDA style commands
-more nets/ports (node numbering && behaviour)
